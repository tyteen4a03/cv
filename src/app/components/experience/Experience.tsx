import type { PropsWithChildren } from "react";

const Experience = ({ children }: PropsWithChildren) => (
    <div className="py-2 prose-h4:mt-0 prose-h4:mb-0">{children}</div>
);

export default Experience;
